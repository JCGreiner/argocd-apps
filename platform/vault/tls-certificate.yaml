apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-tls
  namespace: vault
spec:
  secretName: vault-tls
  issuerRef:
    name: vault-ca
    kind: ClusterIssuer
  dnsNames:
    - vault.vault.svc
    - vault.vault.svc.cluster.local
    - vault.greiner.home