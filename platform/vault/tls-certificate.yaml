apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-tls
  namespace: vault
spec:
  secretName: vault-tls
  issuerRef:
    name: greiner-ca-issuer
    kind: ClusterIssuer
  dnsNames:
    - vault.greiner.home