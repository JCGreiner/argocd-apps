apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: media

resources:
  - namespace.yaml
  - pv-prowlar-config.yaml
  - pvc-prowlar-config.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml