---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: radarr-tls
  namespace: media
spec:
  secretName: radarr-tls
  dnsNames:
    - radarr.greiner.home
  issuerRef:
    kind: ClusterIssuer
    name: greiner-ca-issuer
