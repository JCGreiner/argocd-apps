---
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: homeassistant-config
    files:
      - configuration.yaml
      - solaredge-template.yaml

resources:
  - certificate.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - pvc.yaml