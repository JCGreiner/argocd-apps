---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: lidarr-tls
  namespace: media
spec:
  secretName: lidarr-tls
  dnsNames:
    - lidarr.greiner.home
  issuerRef:
    kind: ClusterIssuer
    name: greiner-ca-issuer
