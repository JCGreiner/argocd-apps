---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: sonarr-tls
  namespace: media
spec:
  secretName: sonarr-tls
  dnsNames:
    - sonarr.greiner.home
  issuerRef:
    kind: ClusterIssuer
    name: greiner-ca-issuer
