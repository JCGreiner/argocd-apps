spec:
  tls:
    - hosts:
        - vault.greiner.home
      secretName: vault-tls

  rules:
    - host: vault.greiner.home
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: vault
                port:
                  number: 8200