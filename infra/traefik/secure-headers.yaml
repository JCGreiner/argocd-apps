apiVersion: traefik.io/v1alpha1
kind: Middleware

metadata:
  name: secure-headers
  namespace: kube-system

spec:
  headers:
    browserXssFilter: true
    contentTypeNosniff: true
    frameDeny: true
    sslRedirect: true